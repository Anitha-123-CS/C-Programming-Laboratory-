#include<stdio.h>
#include<math.h>
int main()
{
double P,R,T,A,CI;
printf("enter P,R,T:);
scanf("%lf%lf%lf",&P,&R,&T);
A=P*((pow((1+R/100)T)));
CI=A-P;
printf("the comound interest:%lf",CI);
return 0;
}
